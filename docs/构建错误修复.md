# 构建错误修复说明

## ❌ 错误 1: Next.js 配置警告

```
⚠ `eslint` configuration in next.config.ts is no longer supported.
⚠ Invalid next.config.ts options detected: Unrecognized key(s) in object: 'eslint'
```

### 原因
Next.js 16+ 不再支持在 `next.config.ts` 中配置 `eslint`。

### 修复
- 从 `next.config.ts` 中移除了 `eslint` 配置
- ESLint 配置应该在 `eslint.config.mjs` 中（已存在）

---

## ❌ 错误 2: TypeScript 类型错误

```
Type error: Cannot find name 'BoardType'.
```

### 原因
`types/game.ts` 中使用了 `BoardType`，但导入方式有问题。

### 修复
- 在 `types/game.ts` 中直接使用字面量类型：`'fate' | 'balance' | 'strategy' | 'custom'`
- 或者从 `@/lib/game/roles` 导入（已在其他地方使用）

---

## ✅ 已修复的文件

1. **`next.config.ts`**
   - 移除了 `eslint` 配置项
   - 添加了注释说明

2. **`types/game.ts`**
   - 将 `board_type?: BoardType` 改为 `board_type?: 'fate' | 'balance' | 'strategy' | 'custom'`
   - 保持类型一致性

---

## 📝 注意事项

1. **ESLint 配置**
   - 现在在 `eslint.config.mjs` 中配置
   - Next.js 16+ 不再支持在 `next.config.ts` 中配置

2. **类型导入**
   - 如果需要在多个地方使用 `BoardType`，可以从 `@/lib/game/roles` 导入
   - 在接口定义中可以直接使用字面量类型

---

**修复完成！现在应该可以成功构建了。** ✅


